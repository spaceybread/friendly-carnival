# will have compiled files and executables
/target/

# These are always ignored by Cargo when it is creating a build,
# but might be useful for local development if you want to
# ignore specific files or directories in your project root.
# For example, if you have a local configuration file:
# .env

# Dependency lock file (optional for libraries, recommended for executables)
# Cargo.lock
